package selection;

public class Calculate2 {

    public Calculate2() {
    }

    public double calculate(double g1, double g2) {
        double sum = g1 + g2;
        {
            return sum;
        }
    }

    public double calcsqrroot(double sqr) {
        return Math.sqrt(sqr);
    }
}






/*
. Maak een klasse Berekening:
        - Geen eigenschappen
        - Constructor
        - 1 methode som() met 2 parameters;
        van deze 2 parameters wordt de som berekend en gereturnd.
        - 1 methode vierkantswortel met 1 parameter
        van deze parameter wordt de vierkantswortel berekend en gereturnd. Dit kan door gebruik te
        maken van de method sqrt() uit de klasse Math, voorbeeld Math.sqrt(9)-> 3
        Maak een Testklasse om deze methods te testen. */
